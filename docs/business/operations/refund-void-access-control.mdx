---
title: Refund & Void Access Control
sidebar_label: Refund & Void Access Control
---

import StepGuide from "@site/src/components/StepGuide";

# Refund & Void Access Control

Ottu uses a permission-based system to control who can perform refund and void operations. **Staff users** must have explicit permissions assigned before they can request refunds or voids, and those requests go through the [Two-Step Authorization](./two-step-authorization.md) approval flow. **Super users** bypass this flow by default.

:::tip
To enable this feature, install the **Permissions Approval** plugin: go to **Ottu Dashboard > Administration Panel > Plugins > Installed Plugins** and add it.
:::

:::note
For accountability and security, only **one user** should be designated as the authorized approver (checker) for refund and void permission requests.
:::

## How It Works

When a staff user is granted refund or void permissions, any operation they initiate enters the [Two-Step Authorization](./two-step-authorization.md) workflow for approval. Pending requests that are not acted upon are automatically canceled after **48 hours** by default. This expiration time is configurable from the backend.

## Assigning Permissions

<StepGuide steps={[
  {
    title: "Open the Users Section",
    description: <>Navigate to <strong>Ottu Dashboard &gt; Administration Panel &gt; Users &gt; Users</strong>.</>,
    image: "/img/business/operations/refund-void-users-section.png",
    imageAlt: "Users section in Administration Panel",
  },
  {
    title: "Select the Target User",
    description: <>Click on the user you want to grant permissions to.</>,
    image: "/img/business/operations/refund-void-select-user.png",
    imageAlt: "Select user",
  },
  {
    title: "Assign Refund and Void Permissions",
    description: <><ol><li>In the user's profile, go to the <strong>Permissions</strong> tab.</li><li>Scroll down to the <strong>User Permissions</strong> section.</li><li>In the <strong>Available User Permissions</strong> list, find:<ul><li><code>payment | Payment transaction | Can do refund</code></li><li><code>payment | Payment transaction | Can do void</code></li></ul></li><li>Move these permissions to the <strong>Chosen User Permissions</strong> list.</li><li>Click <strong>Save</strong>.</li></ol></>,
    image: "/img/business/operations/refund-void-permission-assignment.png",
    imageAlt: "Permission assignment",
  },
]} />

After saving, two permission requests (one for void, one for refund) are added to the [Permission Requests Table](#permission-requests-table).

## Permission Requests Table

The Permission Requests Table is located under the **Tickets** tab on the Ottu Dashboard. This is where the authorized approver (checker) reviews incoming permission requests and either approves or rejects them.

![Permission Requests Table](/img/business/operations/refund-void-permission-requests.png)

When a staff user with the appropriate permissions initiates a refund or void, the request appears here for the checker to act on.

:::warning
If a permission request is neither approved nor rejected within **48 hours**, it is automatically canceled. Adjust this timeout in the backend configuration if your workflow requires more time.
:::

## What's Next?

- [Two-Step Refund & Void Authorization](./two-step-authorization.md) — Understand the full maker-checker approval workflow
- [Operations & Controls](./index.md) — Overview of all operational security features
- [Payment Management](../payment-management/index.md) — View transactions eligible for refund or void
